import LocalTime from "../../src/app/LocalTime";

// Live local-time stamp (Da Nang, GMT+7) in the mono type + orange accent.
export const Default = () => (
  <div style={{ background: "#F0F0EE", padding: 40 }}>
    <LocalTime />
  </div>
);
